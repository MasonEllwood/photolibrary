const styles = (theme) => ({
    root: {
        alignItems: 'center',
        display: 'flex',
        justifyContent: 'center',
        width: '100%',
        flexDirection: 'column',
        height: '100vh',
        width: '100%',
    },
    logo: {
        marginBottom: '20px',
    },
    button: {
        width: '375px',
        borderRadius: '0px',
        backgroundColor: 'white',
    },
});

export default styles;
