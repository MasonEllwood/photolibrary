const styles = (theme) => ({
    home: {
        height: 'calc(100vh - 104px)',
        margin: '20px',
        width: 'calc(100vw - 460px)',
        display: 'flex',
        justifyContent: 'center',
    },
});

export default styles;
