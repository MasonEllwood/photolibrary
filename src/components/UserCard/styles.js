const styles = (theme) => ({
    drawerProfile: {
        display: 'flex',
        alignItems: 'center',
    },
    h3: {
        margin: '0px',
        color: '#ffffff',
    },
    a: {
        color: '#ffffff',
    },
    avatar: {
        marginRight: '8px',
    },
});

export default styles;
