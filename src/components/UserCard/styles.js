const styles = (theme) => ({
    drawerProfile: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-around',
    },
    large: {
        width: 55,
        height: 55,
    },
});

export default styles;
