const styles = (theme) => ({
    menuList: {
        width: `calc(100%)`,
    },
    toolbar: {
        display: 'flex',
        justifyContent: 'space-between',
    },
    menuItems: {
        fontWeight: '900',
        color: '#dedede',
    },
});

export default styles;
