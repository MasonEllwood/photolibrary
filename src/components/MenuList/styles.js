const styles = (theme) => ({
    menuList: {
        width: `calc(100% - 330px)`,
    },
    toolbar: {
        display: 'flex',
        justifyContent: 'space-between',
    },
    menuItems: {
        paddingRight: '15px',
        fontWeight: '500',
        color: '#dedede',
    },
});

export default styles;
